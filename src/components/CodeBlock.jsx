import styled from "styled-components";

const CodeBlock = styled.a`
  padding: 0.2rem;
  background-color: #eee;
  font-size: 14px;
  font-family: Monaco, monospace;
  letter-spacing: -0.05em;
  word-break: normal;
`;

export default CodeBlock;
